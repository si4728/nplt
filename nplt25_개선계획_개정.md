# nplt26.py 코드 개선 재수립 계획

> 대상: `nplt26.py`, `nplt_whois2.py`, `tests/`  
> 기준일: 2026-06-07  
> 목표: 현재 보고서 결과를 보존하면서 크롤링 실패를 관찰 가능하게 만들고, 전역 상태를 줄인 뒤 책임별 모듈로 분리한다.

## 1. 재수립 결론

기존 계획의 방향은 유효하지만 현재 진행 상태와 맞지 않는 항목이 섞여 있다. 이미 완료된 보안·경로·HTTP 세션 작업을 다시 우선순위에 두지 않고, 아직 검증되지 않은 핵심 실행 흐름을 먼저 고정한다.

새 우선순위는 다음과 같다.

1. 대표 사이트의 입력과 결과를 fixture 및 스냅샷으로 고정한다.
2. 빈 `except:`와 불명확한 반환값부터 정리하여 실패 원인을 보존한다.
3. 크롤링 1회에 필요한 전역 상태를 `CrawlState`로 묶는다.
4. DB, HTTP, 분석, 보고서 경계를 분리한다.
5. 측정 결과가 확보된 뒤 이미지 분석과 네트워크 성능을 개선한다.

비동기화, YAML 설정, 전체 타입 적용은 현재 선행 과제가 아니다. 동기 실행의 결과와 오류 모델이 안정된 뒤 판단한다.

## 2. 현재 기준선

2026-06-07 실측 결과다.

| 항목 | 현재 값 |
|---|---:|
| `nplt26.py` | 5,103행 |
| 최상위 함수 | 112개 |
| 빈 `except:` | 34개 |
| `global` 선언 | 20개 |
| `print()` 호출 | 243개 |
| DB 연결 생성 | 4개소 |
| 공용 HTTP 세션 호출 | 13개소 |
| 직접 `requests.get/head/post` | 0개 |
| `verify=False` | 0개 |
| 자동 테스트 | 21개 통과 |
| 컴파일 검사 | 통과 |

확인된 완료 작업:

- `nplt25.py` 원본을 보존하고 `nplt26.py`에서 개선 진행
- import 시 보고서 생성 및 후처리 실행 방지
- DB 접속 설정 환경변수화 및 `.env.example` 제공
- report, temp, image 경로의 `pathlib` 전환
- 공용 `requests.Session`, timeout, 기본 TLS 검증 적용
- WHOIS의 KISA API 우선 및 DNS fallback 구현
- 명확한 유틸리티 버그 일부 수정
- 유틸리티 및 WHOIS 테스트 21개 구성

아직 기준선으로 인정할 수 없는 부분:

- 핵심 `scanWeb()` 흐름의 자동 회귀 테스트 없음
- 실제 DOCX 내용과 주요 지표의 자동 비교 없음
- DB 저장 트랜잭션 테스트 없음
- 대표 HTML fixture와 리다이렉트·오류 응답 fixture 없음
- 실행 시간, 페이지 수, 실패 수의 재현 가능한 벤치마크 없음

## 3. 주요 위험

### P0: 즉시 다룰 위험

1. 빈 `except:` 34개가 프로그래밍 오류까지 숨길 수 있다.
2. `scanWeb()`가 네트워크, 파싱, 통계, 전역 상태 변경을 함께 수행한다.
3. DB 함수의 `finally`에서 연결 생성 실패 시 초기화되지 않은 지역 변수를 참조할 수 있다.
4. HTTP 응답 크기와 최대 페이지 수에 일관된 상한이 없어 메모리와 실행 시간이 급증할 수 있다.
5. 네트워크 함수가 `bytes`, `str`, `None`, 특수 바이트 값을 혼합 반환한다.
6. 보고서 품질을 자동 검증하지 않아 리팩터링 후 조용한 결과 변경을 찾기 어렵다.

### P1: 구조 개선 전 해결할 위험

1. 크롤링 상태가 다수의 전역 리스트, 딕셔너리, 카운터에 분산되어 재실행과 테스트 격리가 어렵다.
2. DB 저장 중 여러 번 commit하여 중간 실패 시 부분 데이터가 남을 수 있다.
3. 정상 진행, 디버그, 오류 출력이 모두 `print()`에 섞여 있다.
4. `requests.headers`라는 비표준 모듈 속성을 만들고 일부 함수가 이를 다시 덮어쓴다.
5. 중복 import, 오래된 주석 코드, 폐기 API 1개가 남아 코드 탐색 비용이 크다.
6. 현재 import 테스트 한 번에 대형 분석 라이브러리까지 로드하므로 CLI 시작과 단위 테스트가 무겁다.

## 4. 실행 계획

## Phase A. 회귀 기준선과 오류 모델

**기간:** 1~2주  
**목표:** 구조 변경 전에 핵심 입력과 결과, 실패 방식을 자동 검증한다.

### 작업

1. `tests/fixtures/`에 최소 4종의 HTML fixture를 추가한다.
   - 정상 정적 사이트
   - 상대 URL, query, fragment가 섞인 사이트
   - 이미지, iframe, 첨부 파일이 있는 사이트
   - 잘못된 HTML과 중복 링크가 있는 사이트
2. 가짜 HTTP 세션으로 HEAD/GET, 302, 404, timeout, 비 HTML 응답을 재현한다.
3. `scanWeb()`에서 네트워크 호출부를 주입 가능하게 만들고 1페이지 회귀 테스트부터 작성한다.
4. DOCX에서 제목, 주요 지표, 이미지 관계를 읽어 검증하는 테스트를 추가한다.
5. 오류 분류를 `network`, `parse`, `file`, `database`, `unexpected`로 정의한다.
6. 빈 `except:` 34개를 위험도 순으로 교체한다.
   - 네트워크 및 URL 처리
   - 파일과 이미지 처리
   - 보고서 생성
   - 핵심 크롤링
7. `robots`, `sitemap`, `myip` 함수의 반환 타입을 명시적 결과 객체 또는 `None`으로 통일한다.

### 종료 조건

- fixture 기반 테스트 10개 이상 추가
- `scanWeb()`의 정상, 리다이렉트, timeout 경로 테스트 통과
- DOCX 핵심 항목 회귀 테스트 통과
- 빈 `except:` 34개 전량 제거
- 예상 오류와 예상 밖 오류가 구분되어 로그 또는 결과에 기록됨

## Phase B. 실행 경계와 자원 안정성

**기간:** 1~2주  
**목표:** HTTP와 DB 자원 사용을 한곳에서 통제한다.

### 작업

1. `HttpClient`를 도입한다.
   - 기본 헤더, connect/read timeout, TLS, 재시도 정책 통합
   - 허용 스킴을 `http`, `https`로 제한
   - 최대 리다이렉트, 최대 응답 크기, 최대 페이지 수 설정
   - HEAD 미지원 시 제한된 GET fallback 제공
2. 외부 IP 조회의 평문 HTTP 사용 여부를 검토하고 HTTPS 가능한 서비스로 교체한다.
3. `DatabaseWriter` 또는 연결 컨텍스트 매니저를 도입한다.
   - 연결과 cursor 초기화 안전화
   - 보고서 1건 저장을 단일 트랜잭션으로 처리
   - 성공 시 commit, 실패 시 rollback
   - SQL과 전체 인자 출력 제거
4. CLI 검증을 추가한다.
   - URL 필수
   - 숫자 옵션 범위
   - Yes/No 및 EN/KR 값 정규화
   - 종료 코드를 성공, 입력 오류, 실행 오류로 구분

### 종료 조건

- 모든 HTTP 요청이 `HttpClient`만 사용
- 응답 크기, 페이지 수, 리다이렉트 제한 테스트 통과
- DB 연결 실패, 쿼리 실패, rollback 테스트 통과
- DB 저장 중 부분 commit 없음
- 잘못된 CLI 입력이 네트워크 호출 전에 거부됨

## Phase C. 상태 캡슐화

**기간:** 2~4주  
**목표:** 한 번의 분석 실행이 독립적인 상태를 갖도록 한다.

### 작업

1. 다음 데이터클래스를 우선 도입한다.
   - `CrawlConfig`: 입력 URL, 제한, 모드, 출력 경로
   - `CrawlState`: 방문 URL, 오류, 카운터, 분석 중간값
   - `PageResult`: 페이지 단위 HTTP 및 분석 결과
   - `CrawlResult`: 보고서와 DB에 전달할 최종 결과
2. 전역 상태를 한 번에 없애지 않고 기능 묶음별로 이전한다.
   - 방문·중복 URL 상태
   - 페이지와 링크 카운터
   - 이미지·스크립트·메타 분석 결과
   - 보고서 레코드
3. `scanWeb(url, parent_url, context)` 형태로 의존성을 명시한다.
4. 두 번 연속 실행해도 첫 실행 상태가 남지 않는 테스트를 추가한다.
5. 진행 출력과 디버그 출력을 `logging`으로 전환한다.

### 종료 조건

- 크롤링 핵심 경로에서 모듈 전역 변경 제거
- 같은 프로세스에서 서로 다른 URL을 연속 실행 가능
- 주요 전역 컬렉션과 카운터 80% 이상 제거
- `print()` 80% 이상 제거하거나 CLI 출력 계층으로 이동
- 기존 fixture 결과와 동일

## Phase D. 책임별 모듈 분리

**기간:** 3~5주  
**목표:** 상태가 안정된 뒤 5,000행 단일 파일을 기능 경계로 분리한다.

기존 `nplt/` 폴더에는 과거 복사본이 있으므로 바로 새 패키지로 사용하지 않는다. 먼저 이름 충돌과 보존 대상을 정리한 뒤 패키지를 만든다.

### 목표 구조

```text
nplt_app/
├── __init__.py
├── __main__.py
├── cli.py
├── config.py
├── models.py
├── http_client.py
├── crawler.py
├── url_utils.py
├── analyzers/
│   ├── page.py
│   ├── esg.py
│   ├── image.py
│   └── website_builder.py
├── reporters/
│   ├── docx.py
│   └── database.py
└── logging_config.py
```

### 분리 순서

1. 설정, 결과 모델, 상수
2. URL 및 문자열 순수 함수
3. WHOIS와 HTTP 클라이언트
4. 페이지 분석 함수
5. DOCX와 DB writer
6. 크롤링 orchestration
7. CLI와 호환 진입점

`nplt26.py`는 전환 기간에 기존 CLI를 새 패키지로 전달하는 얇은 호환 래퍼로 유지한다.

### 종료 조건

- `python -m nplt_app` 실행 가능
- 기존 `python nplt26.py ...` 명령도 동작
- 핵심 모듈이 서로의 전역 변수에 의존하지 않음
- 전체 테스트 통과
- 변경된 핵심 모듈 기준 커버리지 70% 이상

## Phase E. 성능과 운영 품질

**기간:** 2~3주  
**목표:** 측정된 병목만 최적화하고 재현 가능한 실행 환경을 만든다.

### 작업

1. 대표 fixture와 허가된 테스트 사이트에 대해 다음을 기록한다.
   - 총 실행 시간
   - 페이지당 요청 시간
   - 방문·중복·실패 URL 수
   - 최대 메모리
   - 이미지 분석 시간
2. 이미지 픽셀 반복문을 NumPy 연산으로 교체한다.
3. KMeans 입력을 축소 또는 샘플링하고 최대 픽셀 수를 설정한다.
4. 중복 검사에서 리스트를 사용하는 경로를 `set` 또는 dict로 전환한다.
5. `pyproject.toml`에 실행·개발 의존성, 테스트, lint 설정을 정의한다.
6. CI에서 compile, lint, unit test를 실행한다.
7. 동시 요청은 동기 엔진 기준선 이후 제한된 스레드 풀로만 먼저 실험한다.

### 종료 조건

- 최적화 전후 벤치마크 기록
- 이미지 분석 시간 5배 이상 개선 또는 측정 근거로 목표 재조정
- 대표 실행 전체 시간 25% 이상 개선
- 결과 지표와 DOCX 회귀 없음
- 새 환경에서 문서화된 명령으로 설치 및 테스트 가능

## 5. 첫 2주 실행 백로그

작업 순서는 다음으로 고정한다.

| 순서 | 작업 | 산출물 |
|---:|---|---|
| 1 | HTTP fake session과 HTML fixture 4종 작성 | `tests/fixtures/`, 테스트 helper |
| 2 | `scanWeb()` 1페이지 정상·404·timeout 테스트 | 크롤링 경계 테스트 |
| 3 | 네트워크 함수 반환 타입 통일 | 타입이 명확한 함수와 테스트 |
| 4 | 빈 `except:` 중 네트워크·URL 영역 제거 | 구체 예외 및 오류 기록 |
| 5 | DB 연결 컨텍스트 매니저 작성 | 연결 실패 안전성 테스트 |
| 6 | 보고서 저장 단일 트랜잭션화 | commit/rollback 테스트 |
| 7 | DOCX 핵심 항목 검사 테스트 | 보고서 회귀 테스트 |
| 8 | 나머지 빈 `except:` 제거 | 빈 `except:` 0개 |

첫 2주에는 패키지 전체 분리나 비동기화를 시작하지 않는다. 회귀 기준선과 오류 가시성이 확보된 뒤 Phase C로 넘어간다.

## 6. 품질 게이트

각 변경은 아래 조건을 모두 만족해야 병합 가능한 상태로 본다.

- `python -m compileall -q nplt26.py nplt_whois2.py tests` 통과
- `python -m unittest discover -s tests -v` 통과
- 새 버그 수정에는 실패를 재현하는 테스트 포함
- 네트워크 테스트는 외부 인터넷 없이 실행 가능
- 비밀값, 전체 SQL 인자, 개인정보를 로그에 남기지 않음
- 기존 DOCX 핵심 지표 변경 시 의도와 근거 기록

## 7. 별도 운영 작업

다음은 코드 리팩터링과 별도 일정으로 처리한다.

- 현재 DB 비밀번호 교체
- DB `root` 계정을 최소 권한 전용 계정으로 전환
- 과거 파일과 배포본의 자격증명 노출 이력 점검
- DB 방화벽과 접속 허용 IP 검토
- 외부 사이트 분석 시 robots 정책, 요청 간격, 이용 약관 검토

## 8. 전체 예상 일정

| 단계 | 핵심 결과 | 예상 기간 |
|---|---|---:|
| Phase A | 회귀 기준선과 오류 모델 | 1~2주 |
| Phase B | HTTP·DB 실행 경계 | 1~2주 |
| Phase C | 전역 상태 캡슐화 | 2~4주 |
| Phase D | 책임별 패키지 분리 | 3~5주 |
| Phase E | 성능·설치·CI | 2~3주 |

총 예상 기간은 9~16주다. Phase A 종료 시 fixture 수, 실제 사이트 처리 시간, DB 테스트 가능 범위를 기준으로 다시 산정한다.
