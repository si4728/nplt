# nplt26 ESG 키워드 검토 보고서

## 1. 검토 범위

- ESG 키워드 목록: `nplt26.py`의 `nplt_esg_word`
- 키워드 검색: `check_stringAll()`
- 집계: `counting_esg_word()`
- 보고서 및 DB 출력: `esg_count`

## 2. 목록 현황

| 항목 | 결과 |
|---|---:|
| 전체 항목 | 471 |
| 고유 항목 | 469 |
| 영어 항목 | 231 |
| 한글 항목 | 240 |
| 완전 중복 | 2종 |
| 앞뒤 공백 항목 | 3 |
| 빈 항목 | 0 |

완전 중복 항목:

- `HEALTH AND SAFETY`
- `CORPORATE GOVERNANCE`

앞 공백이 포함된 항목:

- ` 이해관계자 참여`
- ` 이해관계자 자본주의`
- ` 이해관계자 요구사항`

## 3. 데이터 품질 문제

즉시 정리 가능한 표기 오류:

| 현재 값 | 권장 값 |
|---|---|
| `회사 윤 리` | `회사 윤리` |
| `사회적 가치 공 동 창출` | `사회적 가치 공동 창출` |
| `환경  책임 강화` | `환경 책임 강화` |
| `에코 로지` | `생태학` 또는 `에콜로지` |
| `3중 기준` | `트리플 바텀 라인` 또는 `경제·사회·환경 성과` |

표기 통일이 필요한 예:

- `지속가능성` / `지속 가능성`
- `지배구조` / `지배 구조`
- 앞 공백이 있는 `이해관계자 참여` / 정상 표기의 `이해관계자 참여`

목록에는 동일 개념의 번역어, 유사어, 세부 문구가 각각 독립 키워드로 혼재한다. 따라서 같은 의미가 여러 항목으로 나뉘거나 한 문장에서 여러 키워드가 동시에 검출될 수 있다.

## 4. 집계 로직 문제

### 4.1 대소문자 구분

현재 검색은 `substring in string` 방식으로 대소문자를 구분한다. 영어 키워드는 모두 대문자이므로 웹 페이지 본문이 `Sustainability`, `climate change`처럼 작성되면 검출되지 않는다.

### 4.2 부분 문자열 중복 검출

단순 부분 문자열 검색이므로 다음과 같은 문구는 상위·하위 키워드가 함께 집계될 수 있다.

- `SOCIAL IMPACT ASSESSMENT`
- `SOCIAL IMPACT`
- `SOCIAL`

또한 `SOCIAL`, `ETHICS`, `COMPLIANCE`, `DISCLOSURE`, `ENVIRONMENT`처럼 범위가 넓은 단어는 ESG 문맥과 무관한 문장도 검출할 가능성이 있다.

### 4.3 집계 의미와 보고서 명칭 불일치

`counting_esg_word()`는 한 페이지 안에서 같은 키워드가 여러 번 나와도 1회만 증가시킨다. 현재 값은 단어 빈도가 아니라 **키워드를 포함한 페이지 수**에 가깝다.

영문 보고서 제목 `Frequency ESG word list in website`는 실제 집계 의미와 다르다. 다음처럼 바꾸는 것이 정확하다.

- 한글: `ESG 키워드 포함 페이지 순위`
- 영문: `Pages containing ESG keywords`

### 4.4 정규화 부재

검색 전에 다음 정규화가 수행되지 않는다.

- 유니코드 정규화
- 연속 공백 축약
- 앞뒤 공백 제거
- 영문 대소문자 통일

웹 문서의 줄바꿈, 탭, 다중 공백에 따라 구문형 키워드가 누락될 수 있다.

### 4.5 결과 정렬과 DB 저장

- Word 보고서는 집계 수 기준으로 정렬한다.
- DB 저장은 `esg_count` 삽입 순서로 제한 개수만 저장하므로 상위 키워드가 저장된다는 보장이 없다.
- DB의 49자 잘라내기는 서로 다른 긴 키워드를 동일하게 만들 가능성이 있다.

## 5. 개선 우선순위

### Phase 1: 검색 정확도 보정

1. 본문과 키워드에 Unicode NFKC, 공백 축약, `casefold()`를 적용한다.
2. 영어 단일어는 단어 경계 기반으로 검색한다.
3. 결과 명칭을 `포함 페이지 수`로 변경한다.
4. DB 저장 전에 집계 수 기준으로 정렬한다.
5. 완전 중복, 앞 공백, 명백한 오탈자를 정리한다.

### Phase 2: 중복 집계 제어

1. 한 문장 안에서 겹치는 문구는 가장 긴 키워드를 우선하는 방식을 검토한다.
2. `page_count`와 실제 `occurrence_count`를 분리한다.
3. 일반 단어와 ESG 전용 구문을 `core`, `extended` 등급으로 구분한다.

### Phase 3: 키워드 구조화

Python 배열 대신 JSON 또는 CSV로 분리하고 다음 필드를 둔다.

| 필드 | 용도 |
|---|---|
| `id` | 개념 고유 ID |
| `category` | E, S, G 또는 공통 |
| `canonical` | 대표 용어 |
| `aliases` | 한글·영문·유사어 |
| `match_type` | 단어, 구문, 정규식 |
| `enabled` | 사용 여부 |
| `profile` | core 또는 extended |

대표 개념 단위로 별칭을 합산하면 한글·영문 표현이 달라도 동일 ESG 개념으로 보고할 수 있다.

## 6. 권장 테스트

- 영어 대문자·소문자·혼합 표기 검출
- 한글 다중 공백과 줄바꿈 정규화
- `SOCIAL IMPACT ASSESSMENT`의 중첩 집계 정책
- 한 페이지 반복 출현 시 페이지 수와 출현 횟수 분리
- 중복 키워드가 한 번만 집계되는지 확인
- 집계 상위 순서대로 Word와 DB에 출력되는지 확인
- ESG와 무관한 일반 문장에서 광범위 키워드의 오검출 확인

## 7. 결론

현재 목록은 ESG 관련 용어를 폭넓게 수집했지만, 분류와 별칭 구조 없이 평면 배열로 관리되고 있다. 가장 큰 문제는 목록 개수보다 검색 방식이다. 대소문자 구분으로 영어 키워드가 누락되고, 단순 부분 문자열 방식으로 유사·포함 키워드가 중복 집계될 수 있다.

우선 Phase 1을 적용해 기존 보고서와의 호환성을 유지하면서 검출 정확도와 집계 의미를 바로잡고, 이후 중첩 정책과 E/S/G 분류 체계를 도입하는 순서가 적절하다.
